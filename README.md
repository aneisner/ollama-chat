# Ollama Chat - Cockpit Modul

Ein leichtgewichtiges, eigenständiges Cockpit-Modul, um direkt in der Webkonsole mit lokalen Ollama-Modellen zu chatten.

## Installation

1. Kopiere den Ordner nach `/usr/share/cockpit/`:
```bash
   sudo cp -r ../ollama-chat /usr/share/cockpit/
